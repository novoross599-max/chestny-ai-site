<section class="hero" id="top">
  <div class="container hero-grid">
    <div class="hero-copy">
      <div class="eyebrow"><span class="live-dot"></span> Дневник реального обучения</div>
      <h1>Осваиваю ИИ с нуля и показываю весь путь <em>честно</em></h1>
      <p>Учусь работать с ChatGPT и Codex, создавать сайты, приложения и собственные ИИ-инструменты — простым языком, на практике и без обещаний мгновенного успеха.</p>
      <div class="hero-actions"><a class="btn btn-primary" href="https://t.me/AI_Dnevnik_K" target="_blank" rel="noopener noreferrer">Читать дневник <span aria-hidden="true">↗</span></a><a class="text-link" href="#results">Посмотреть результаты ↓</a></div>
      <div class="principles"><span>Без сложных терминов</span><span>Только личный опыт</span><span>От первого шага</span></div>
    </div>
    <div class="diary" aria-label="Фрагмент дневника обучения">
      <div class="diary-top"><span>Запись № 07</span><time datetime="2026-08-19">19 августа</time></div>
      <div class="diagram">
        <div class="node idea">идея</div><span>→</span><div class="node">Codex</div><span>→</span><div class="node result">сайт</div>
      </div>
      <h2>Сегодня сайт перестал быть просто идеей.</h2>
      <p>Разобрался со структурой Astro, создал компоненты и увидел результат в браузере. Не всё получилось сразу — и именно поэтому этот опыт полезен.</p>
      <div class="note"><b>Вывод дня</b><span>Маленький рабочий результат ценнее десятка просмотренных уроков.</span></div>
      <svg class="scribble" viewBox="0 0 150 70" aria-hidden="true"><path d="M5 60C48 64 110 59 133 25M116 27l19-5-3 19" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" /></svg>
    </div>
  </div>
</section>
<style>
  .hero { min-height: calc(100vh - 76px); display: grid; align-items: center; padding: clamp(3rem, 7vw, 6rem) 0; overflow: hidden; }
  .hero-grid { display: grid; grid-template-columns: 1.15fr .85fr; align-items: center; gap: clamp(2.5rem, 6vw, 6rem); }
  .eyebrow { display: inline-flex; align-items: center; gap: .6rem; margin-bottom: 1.3rem; padding: .48rem .8rem; border: 1px solid var(--line); border-radius: 999px; background: rgba(255,255,255,.45); color: var(--primary); font-size: .78rem; font-weight: 800; }
  .live-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--accent); box-shadow: 0 0 0 5px rgba(217,150,69,.16); }
  h1 { max-width: 820px; margin-bottom: 1.5rem; } h1 em { color: var(--primary); font-family: var(--font-heading); font-weight: 400; }
  .hero-copy > p { max-width: 720px; font-size: clamp(1.05rem, 1.8vw, 1.28rem); }
  .hero-actions { display: flex; align-items: center; gap: 1.25rem; margin-top: 2rem; } .text-link { color: var(--ink); font-weight: 800; text-underline-offset: 5px; }
  .principles { display: flex; flex-wrap: wrap; gap: .6rem 1.5rem; margin-top: 2.3rem; color: var(--muted); font-size: .78rem; font-weight: 700; } .principles span::before { content: '✓'; margin-right: .45rem; color: var(--primary); }
  .diary { position: relative; padding: clamp(1.5rem, 4vw, 2.5rem); border: 1px solid #d6d1c5; border-radius: 8px 28px 28px 8px; background: #fffdf7; box-shadow: 12px 14px 0 #d8c8ae, var(--shadow); transform: rotate(1.5deg); }
  .diary::before { content: ''; position: absolute; left: 34px; top: 0; bottom: 0; width: 1px; background: #e6a8a0; opacity: .6; }
  .diary-top { display: flex; justify-content: space-between; color: var(--muted); font-size: .75rem; font-weight: 700; letter-spacing: .08em; text-transform: uppercase; }
  .diagram { display: flex; align-items: center; gap: .5rem; margin: 2rem 0 1.5rem 1.2rem; } .node { padding: .5rem .65rem; border: 1px solid var(--line); border-radius: 8px; font-size: .72rem; font-weight: 800; } .idea { background: var(--accent-soft); } .result { color: white; background: var(--primary); }
  .diary h2 { margin-left: 1.2rem; font-family: var(--font-heading); font-size: clamp(1.65rem, 3vw, 2.25rem); font-weight: 400; } .diary > p { margin-left: 1.2rem; font-size: 1rem; }
  .note { display: grid; gap: .3rem; margin: 1.5rem 0 0 1.2rem; padding: 1rem; border-left: 3px solid var(--accent); background: #f7f0e4; font-size: .82rem; } .note span { color: var(--muted); }
  .scribble { position: absolute; right: -45px; bottom: -45px; width: 130px; color: var(--accent); }
  @media (max-width: 900px) { .hero { min-height: auto; } .hero-grid { grid-template-columns: 1fr; } .diary { max-width: 620px; transform: none; } }
  @media (max-width: 560px) { .hero-actions { align-items: stretch; flex-direction: column; } .text-link { text-align: center; } .principles { flex-direction: column; } .diary { box-shadow: 7px 8px 0 #d8c8ae; } .scribble { display: none; } }
</style>
